import React from 'react'
import Link from 'gatsby-link'

import styles from './styles/footer.module.css'

export default () => {
  return (
    <footer className={styles.footer}>
      <div className={styles.footerItem}>Contact info</div>
    </footer>
  )
}
