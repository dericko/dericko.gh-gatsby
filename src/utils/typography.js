import Typography from 'typography'

const typography = new Typography({
  baseFontSize: '18px',
  baseLineHeight: 1.45,
  headerFontFamily: ['Helvetica', 'Arial', 'sans-serif'],
  bodyFontFamily: ['Helvetica', 'Ariel', 'sans-serif'],
})

export default typography
